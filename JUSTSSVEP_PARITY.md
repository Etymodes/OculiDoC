# OculiDoC 与 JustSsvep 1.0.1 功能对齐

本表基于 Tieying 现场只读端口审计和所附 JustSsvep 1.0.1 包。它区分“已由证据确认”与
“仍需设备方确认”，避免把 CH340 USB 转串口芯片误写成脑电设备型号，也不把包内随机
`mock_ssvep_algorithm()` 当作有效识别。

| JustSsvep 可见能力 | OculiDoC v0.1.3 实现 | 验收证据/边界 |
| --- | --- | --- |
| CH340/COM4 采集链 | 连接 JustSsvep 已运行的本机 WebSocket `127.0.0.1:12991` | 不抢占 COM4；波特率和二进制帧未知时不直连 |
| 8 通道实时波形 | 解析 `FP1,FP2,O1,O2,Oz,PO3,PO4,POz` 原始计数并选择任务通道 | 非 8 通道明确报 `unsupported_device` |
| 开始/停止采集 | 发送已观测的 `usbPortSwitch` 开始/停止命令 | 异常退出也在连接关闭前尽力停止 |
| SSVEP 标记 | 发送已观测的 `ssvep_mark` 开始/停止命令 | 忽略 JustSsvep 返回的随机分类，OculiDoC 自行解码 |
| 连接、电量、通道接触质量 | 患者任务窗显示质量与电量；结果保存逐试次 telemetry 和质量门禁 | 不保存 brain MAC 等私有标识 |
| 原始 CSV | 保留去标识桥接计数；每试次另存无损 NPZ 与规则 CSV | 旧 CSV 只用于回放复核；标签未获证据时不晋级患者模型 |
| CCA/FBCCA | CCA 基线、FBCCA 默认主算法 | 记录全部得分、概率、置信度、差值与拒绝原因 |
| 训练式识别 | TRCA/eTRCA 患者模型 | 模拟模型不能进入真实患者；模型患者/通道/频率/采样率均校验 |
| 滚动自适应 | 有标签频扫生成候选，时间后置 holdout 决定是否晋级 | 每频率至少 4 次；准确率/覆盖率 ≥0.75；相对旧模型下降 ≤0.02 |
| 二/四目标任务 | 单目标、二目标、四目标、频扫和验证均可独立运行 | 配置与报告不进入既有眼动纵向趋势 |
| 选择后任务反馈 | “是/否”二选一沟通：刺激→采集→质量→解码→可见反馈→拒绝重试 | 每次选择写入 `task_outcomes`，无标签自预测不参与训练 |
| 外骨骼、机器狗、轮椅动作 | 不实现 | 属于安全关键外部执行器，超出当前 OculiDoC 医疗交互边界 |
| DEBUG 随机结果/分数修改 | 不移植 | JustSsvep 包内实现是随机 mock，不能作为算法或验收依据 |

## 尚未确认的硬件参数

- 脑电设备正式品牌/型号与注册信息；
- 串口波特率、奇偶校验和二进制帧结构；
- 原始计数到 µV 的厂商换算系数；
- 设备实际采样率是否精确为 250 Hz；
- 硬件触发或其他高于时间戳的同步能力。

OculiDoC 允许操作者显式填写 µV/计数换算，但只有勾选“已由厂商/设备协议确认”后，真实
来源才具备正式报告和患者模型晋级资格。此约束不阻止原始数据落盘和工程算法复核。
