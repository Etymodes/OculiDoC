# OculiDoC v0.1.3 多模态神经交互架构

## 版本边界

v0.1.3 在既有患者、眼动任务、实验会话和报告链旁增加独立神经信号链。眼动、SSVEP、
被动 EEG 与运动想象可以属于同一患者的长期档案，但每次任务各自采集、各自解释、各自
报告。本版本不把多个范式融合成控制输出，也不包含机器人、外骨骼或拼写器。

## 分层

| 层 | 代码 | 职责 |
| --- | --- | --- |
| 患者默认值 | `signals/profile.py` | 版本化保存患者可用设备、通道、采样率、范式和历史频率 |
| 会话快照 | `signals/snapshot.py` | 在任务启动前冻结配置并计算 SHA-256，报告只读取快照 |
| 标准 EEG | `signals/models.py` | `EEGSampleBlock`、质量和统一标记，不暴露厂商字段 |
| 信号来源 | `signals/sources.py` | 标准本地桥、NPZ 回放和明确隔离的工程模拟 |
| Mylian 兼容 | `devices/eeg/adapters/mylian` | 将厂商桥 JSON 转换为标准 EEG；能力错误不触发模拟回退 |
| SSVEP | `bci/ssvep` | CCA、FBCCA、TRCA、eTRCA、校准、评估、刺激配置和注册表 |
| 独立任务 | `signal_tasks` | EEG 质量、SSVEP、MI 的采集、刺激 UI、结果和子进程入口 |
| 会话/报告 | `application/signal_*` | 绑定患者会话、登记产物、生成工程或正式来源报告 |

## 标准 EEG 合同

`EEGSampleBlock` 的核心字段为：设备 ID、起始时间戳、采样率、通道名、微伏矩阵、质量、
标记和 `simulated` 来源标记。矩阵形状固定为“通道 × 样本”，构造后只读。标准 NPZ
禁止 pickle，回放保留原始 `simulated` 标记，不能借回放去除模拟来源身份。

Mylian 的 `brainPayload`、`targetFre_est`、`frequencyFeaturesStr` 是兼容层私有字段。
只有 `brainPayload` 被转换为标准微伏矩阵；厂商估计频率和特征字符串不进入算法、会话
快照或报告，避免把不可审计的厂商结果误当作 OculiDoC 解码结果。

医院历史压缩包本轮只用于核对既有眼动换行 JSON 协议。包内没有可确认的 EEG/SSVEP
实现，因此没有复制其中运行时、DLL、日志或患者数据，也没有据此宣称 Mylian 实机可用。

## 任务能力矩阵

| 独立任务 | 范式 | 频率约束 | 解码 |
| --- | --- | --- | --- |
| EEG 信号质量 | 被动 EEG | 无 | 有效率、标准差、峰峰值和设备质量 |
| SSVEP 频率扫描 | SSVEP | 1 个以上、界面配置 | CCA/FBCCA 或患者校准 TRCA/eTRCA |
| SSVEP 单目标 | SSVEP | 恰好 1 个 | 同上 |
| SSVEP 二目标 | SSVEP | 恰好 2 个 | 同上 |
| SSVEP 四目标 | SSVEP | 恰好 4 个 | 同上 |
| SSVEP 解码验证 | SSVEP | 1 个以上、界面配置 | 逐试次得分、置信度、拒绝和准确率 |
| 运动想象独立协议 | MI | 无 | 8–13 Hz 与 13–30 Hz 特征；不输出分类控制 |

算法不包含任务频率常量。`SsvepStimulusConfig` 同时供屏幕刺激和解码器使用，记录频率、
相位、刷新率、屏幕、谐波、窗口和延迟。FBCCA 是界面默认算法；TRCA/eTRCA 必须提供与
患者、频率、采样率和通道匹配的校准模型。模拟校准模型不能用于正式患者会话。

## 来源与失败策略

| 来源 | 用途 | 失败行为 |
| --- | --- | --- |
| Mylian 本地桥 | 首个厂商硬件兼容边界 | 运行时/符号缺失、设备不支持、许可不足均明确失败 |
| 标准本地 JSON 桥 | 其他采集进程输出标准块 | 文件、字段或 JSON 无效时明确失败 |
| 标准 NPZ 回放 | 可复现算法验证 | 文件/模式无效时明确失败，并保留模拟身份 |
| 工程模拟 | `Beta00` 回归与演示 | 确定性生成，不允许真实患者启动 |

没有“硬件失败后自动模拟”的路径。Mylian SDK、DLL、许可和厂商进程始终是外部部署项，
不随 OculiDoC 分发。当前适配器和自动化测试证明兼容合同与失败隔离，不等同于 Augusta
上的真实 Mylian 设备、驱动、许可或床旁环境验收。

## 时间同步

`SignalCoordinator` 使用固定优先级：硬件触发 > LSL > 单调时间戳 > 软件估计。v0.1.3
内置来源至少提供时间戳；只有外部桥明确提供更强同步能力时才可升级方法。报告同时记录
本次方法和完整优先策略。

## 报告与模拟隔离

信号报告记录设备、通道、采样率、算法名称与版本、完整参数、配置频率、逐试次得分、
置信度、拒绝原因、模型身份和快照哈希。信号报告不会进入眼动纵向趋势计算。

凡任一采集块标记 `simulated=true`，整次任务即为模拟来源：

- 只允许内置 `Beta00`；
- `report_eligible=false`；
- 标题和正文醒目标注“工程模拟”；
- 不得进入真实患者临床报告、科研统计或临床决策。

## Augusta 验收边界

源码门禁应覆盖 Ruff、完整 pytest、编译、包内资源烟测，以及 Beta00 的 EEG 质量、
SSVEP CCA/FBCCA 和 MI 无界面端到端运行。界面人工验收应核对主页多选、P300 禁用、
配置校验、刺激窗口、停止任务、历史明细和工程水印报告。

真实硬件验收必须另行记录设备型号、外部桥版本、DLL/符号、驱动、许可、通道、采样率、
显示刷新率、同步方式和短时原始块。没有这份 Augusta 现场结果前，不得把“适配器已实现”
写成“Mylian 已验收”。
