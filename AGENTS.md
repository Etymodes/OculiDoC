# OculiDoC 仓库工作约定

## 复核基线

日常开发与复核的事实源依次为：

1. 当前工作树及其未提交改动；
2. GitHub 仓库 `Etymodes/OculiDoC` 的当前 `main`；
3. 仓库内已跟踪的测试、`README.md`、`NOTICE.md` 和公开兼容性说明。

开始任务时先只读检查 `git status --short --branch`、`git remote -v` 和当前提交，
再通过 GitHub 复核远端。不得为了“重新理解项目”而反复解压、扫描或比较
`医院眼动仪.zip`。该压缩包只属于历史行为证据；除非用户明确要求重新审计某项
尚未在当前仓库和测试中固化的历史行为，否则不再把它作为工作流输入。

保留现有旧设备兼容适配器不等于继续依赖旧压缩包。兼容行为应由当前代码、测试和
现场实机结果维护。

## 品牌命名

当前没有正式公开的中文名，对外显示名统一为 `OculiDoC`。

“沃离笃”仅保留为内部备用中文名，不得默认用于公开界面、报告、安装包或权属声明。
除非团队以后重新确认，否则不得把它写成正式中文名。“离”取“离为目”；“笃”兼取
马行顿迟、病势沉重及 `DoC` 的入声听感，但这些含义不足以让使用者主动联想到眼动或
意识障碍，也是它未被正式采用的原因。

仓库名、Python 包名、可执行文件名、安装目录、更新地址、数据格式标识及其他技术兼容
标识继续使用 `OculiDoC`。

## 变更安全

- 不覆盖、不清理用户已有的未提交改动。
- 不提交患者数据库、原始眼动数据、日志、许可证、密钥、专有 DLL/EXE/PDB 或安装包。
- 新硬件能力必须先做运行时能力检测；可选信号缺失、许可不足或驱动拒绝时，既有注视
  任务和已验收报告链必须继续工作。
- Tobii SDK、运行时和工具均视为外部依赖，不随 OculiDoC 分发。接入前必须核对设备型号、
  当前官方许可、允许用途和数据存储/传输权限。
- 新 Python 依赖只在对应适配器已经实现且有测试时加入；不得为了未来占位预装 SDK。

## OpoinThesis

`OpoinThesis` 是项目内的有意构词：取古希腊语形式 `ὀποῖν θέσις`
（`opoîn thésis`，项目释义为“双眼的位置”），同时在英语听感上近似
`open thesis`，形成“开放研究”的双关。

当前版本的边界仍是主观眼位观察：不自动评分、不生成合格结论、不持久化、不进入正式
患者报告。名称同时保留清晰扩展缝，供未来在获得合规数据源后接入智能眼位分析、患者
个体化自适应模型，以及与 EEG/BCI 的时间同步接口。不得把这些未来能力写成当前已实现
功能。

## 验证

先运行与改动直接相关的测试，再运行完整测试、Ruff 和 Mypy。涉及设备能力时，至少覆盖：

- 可选信号可用；
- DLL/符号缺失；
- 设备或驱动不支持；
- 许可不足；
- 回退后既有注视采集与报告仍可用。
